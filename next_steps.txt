complete the logging test and solve its issues -- done
complete the access token generator file and use the generated access token in the ingestion album search file
add retrying to api calls 
simulate a scenario in which you can only make 60 calls per minute in your api calls 

--releases albums >
--- album_type(different values), artists(list of dicts of dicts), name, release_date, total_tracks, id, uri, external_urls


-- update the path of the bronze, silver, ... layers using env varialbes and .... 
-- create the bronze layer using spark